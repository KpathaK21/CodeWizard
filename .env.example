# OpenAI API Key (Optional - users can provide their own keys through the UI)
# Only needed if you want to provide a default fallback API key
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (Optional - users can provide their own keys through the UI)
# Only needed if you want to provide a default fallback API key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Flask configuration
FLASK_APP=app.py
FLASK_ENV=development

# CodeWizard configuration
APP_NAME=CodeWizard